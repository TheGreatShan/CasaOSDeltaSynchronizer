# CasaOS Delta Synchronizer

This small program synchronizes files with a remote server, which is run with CasaOS.
This is to be installed on the client side, and will synchronize files with the server.
